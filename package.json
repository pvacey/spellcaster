{
  "name": "discard",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.2",
    "vitest": "2.1.8"
  },
  "dependencies": {
    "cookie-signature": "^1.2.2",
    "itty-router": "^5.0.18",
    "wrangler": "^3.93.0"
  }
}
